name: 'Save a cache'
description: 'Save Cache artifacts like dependencies and build outputs to improve workflow execution time'
author: 'GitHub'
inputs:
  path:
    description: 'A list of files, directories, and wildcard patterns to cache'
    required: true
  key:
    description: 'An explicit key for saving the cache'
    required: true
  upload-chunk-size:
    description: 'The chunk size used to split up large files during upload, in bytes'
    required: false
  enableCrossOsArchive:
    description: 'An optional boolean when enabled, allows windows runners to save caches that can be restored on other platforms'
    default: 'false'
    required: false
  azure-connection-string:
    description: 'Azure Storage connection string. When provided, cache will be saved to Azure Blob Storage instead of GitHub Actions cache.'
    required: false
  azure-container-name:
    description: 'Azure Blob container name for cache storage'
    required: false
    default: 'github-actions-cache'
runs:
  using: 'node24'
  main: '../dist/save-only/index.js'
branding:
  icon: 'archive'
  color: 'gray-dark'
